build/
out/
.vs/
*.user
*.aps
*.pdb
*.ilk
*.exp
*.obj
*.idb
CMakeSettings.json
.DS_Store
build-preview/

# The icon workspace: Figma pieces, reference shots and every size the design
# tool exports. Only the frames the .ico is actually cut from are kept, and
# those live in res/icon/.
logo/

# Working notes that are not documentation. Deliberately unpublished.
notes/
